using System;

class Program
{
    static void Main(string[] args)
    {
        List<int> list_of_numbers = new List<int> {1, 1, 1};
        
        int current_index = 0;
        int final_index = list_of_numbers.Count - 1;

        while (current_index != final_index)
        {
            
            (list_of_numbers[current_index]);
            break;
        }
        Console.Writeline();
    }
}